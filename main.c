#include "zhost.h"

int main( int argc, char *argv[] ) {
    UT_RunApp( "zloedi", 
               "zhost",
               "zhost skeleton",
               colorrgb( 0.1f, 0.1f, 0.1f ),
               NULL,
               NULL,
               NULL,
               NULL,
               NULL );
    return 0;
}
